# Web 框架 & API
Django>=5.1,<6.0
djangorestframework>=3.15
drf-spectacular>=0.28,<1.0
django-cors-headers>=4.4
python-dotenv>=1.0
cryptography>=43.0,<47.0
requests>=2.32,<3.0
cos-python-sdk-v5>=1.9
channels>=4.1,<5
daphne>=4.1,<5
langgraph>=1.0,<2.0
python-docx>=1.1,<2.0
reportlab>=4.2,<5.0

# 第1层 数据底座
duckdb>=1.1
psycopg2-binary>=2.9
openpyxl>=3.1
xlrd>=2.0.1

# 说明:
# 第4层 LangGraph 以进程内 Python 库运行，不启动独立 LangGraph HTTP 服务。
# 第2层 LightRAG 仍可按部署需要替换为完整 SDK:
#   lightrag-hku
#   openai>=1.0            # LLM 调用(意图识别/生成)
